# 🤖 Telegram Бот для миниприложения "Итоги школьного года"

Python бот для Telegram с админ-панелью и интеграцией миниприложения.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

или

```bash
pip3 install -r requirements.txt
```

### 2. Настройка

Токен бота уже указан в коде, но лучше использовать переменные окружения:

**Linux/Mac:**
```bash
export BOT_TOKEN="7962013308:AAFbwbd8B99dVU13Za8967ON2S52B9j40Lg"
export ADMIN_IDS="123456789,987654321"  # Ваши ID через запятую (необязательно)
export WEBAPP_URL="https://ваш-домен.ddns.net/"  # Ваш DDNS URL
```

**Windows (PowerShell):**
```powershell
$env:BOT_TOKEN="7962013308:AAFbwbd8B99dVU13Za8967ON2S52B9j40Lg"
$env:ADMIN_IDS="123456789,987654321"
$env:WEBAPP_URL="https://ваш-домен.ddns.net/"
```

**Или создайте файл `.env` (не коммитьте в Git!):**
```
BOT_TOKEN=7962013308:AAFbwbd8B99dVU13Za8967ON2S52B9j40Lg
ADMIN_IDS=123456789,987654321
WEBAPP_URL=https://ваш-домен.ddns.net/
```

### 3. Запуск бота

```bash
python3 bot.py
```

или

```bash
python bot.py
```

## 📋 Основные команды

### Для пользователей:
- `/start` - Запустить бота и получить приветственное сообщение
- `/help` - Справка по использованию
- `/stats` - Личная статистика

### Для администраторов:
- `/admin` - Открыть админ-панель
- `/setwebapp <url>` - Быстро установить URL миниприложения

## ⚙️ Админ-панель

Админ-панель доступна через команду `/admin` и включает:

- **📊 Статистика бота** - общая статистика (пользователи, сообщения)
- **👥 Пользователи** - список зарегистрированных пользователей
- **📢 Рассылка** - отправка сообщений всем пользователям
- **🔗 Установить URL приложения** - настройка URL миниприложения
- **⚙️ Настройки** - просмотр настроек бота

## 🔧 Как узнать свой Telegram ID

Чтобы добавить себя в администраторы:

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему `/start`
3. Скопируйте ваш ID (число)
4. Укажите его в переменной `ADMIN_IDS` или в коде

## 📦 Структура файлов

```
enazvalox/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── config.json         # Конфигурация (создаётся автоматически)
├── stats.json          # Статистика (создаётся автоматически)
└── README_BOT.md       # Эта инструкция
```

## 🌐 Настройка для DDNS

Если вы используете DDNS для хостинга миниприложения:

1. Загрузите файлы миниприложения на ваш сервер
2. Настройте DDNS (например, No-IP, DuckDNS)
3. Установите SSL сертификат (обязательно для Telegram!)
4. Укажите URL через команду `/setwebapp` или переменную окружения

**Пример:**
```
/setwebapp https://mybot.ddns.net/
```

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте токен бота в Git!
- Используйте `.gitignore` для исключения конфиденциальных файлов
- Храните токен в переменных окружения или `.env` файле
- Для продакшена используйте базу данных вместо JSON файлов

## 🐳 Запуск в фоне (Linux/Mac)

### Вариант 1: screen
```bash
screen -S telegram_bot
python3 bot.py
# Нажмите Ctrl+A, затем D для выхода
# Вернуться: screen -r telegram_bot
```

### Вариант 2: nohup
```bash
nohup python3 bot.py > bot.log 2>&1 &
```

### Вариант 3: systemd (для сервера)
Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/enazvalox
ExecStart=/usr/bin/python3 /path/to/enazvalox/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

## 📝 Логирование

Бот автоматически логирует все события. Для просмотра логов:

```bash
tail -f bot.log  # если используете nohup
```

## ❓ Решение проблем

### Бот не отвечает
- Проверьте правильность токена
- Убедитесь, что интернет соединение работает
- Проверьте логи на наличие ошибок

### Ошибка с SSL сертификатом
- Убедитесь, что ваш DDNS имеет валидный SSL сертификат
- Используйте Let's Encrypt для бесплатного SSL

### Бот падает
- Проверьте версию Python (нужен 3.8+)
- Убедитесь, что все зависимости установлены
- Проверьте логи на ошибки

## 🔄 Обновление

```bash
pip install --upgrade aiogram
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность токена и URL

---

**Удачи с ботом! 🤖✨**

